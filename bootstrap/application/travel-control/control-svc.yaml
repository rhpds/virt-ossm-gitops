apiVersion: v1
kind: Service
metadata:
  name: travel-control
  namespace: travel-control
  labels:
    app: travel-control
spec:
  ports:
    - port: 8080
      name: http
  selector:
    kubevirt.io/domain: travel-control
